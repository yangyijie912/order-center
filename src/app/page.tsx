import styles from './page.module.css';

// 根页面（示例）：默认由 create-next-app 生成，用作占位和示例链接
export default function Home() {
  return (
    <div className={styles.page}>
      <main className={styles.main}></main>
      <footer className={styles.footer}></footer>
    </div>
  );
}
